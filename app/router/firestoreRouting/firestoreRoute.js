const express = require('express');
const router = express.Router();
const firestoreController = require('../../controller/firestoreController');

// router.post('/dataUser', firestoreController.dataUser);

module.exports = router;